# 2026

Version history for 2026 — newest first. Each row is an exact, revertable point
(a tagged commit). Recorded automatically on every commit; never edit by hand.

| Version | Time | Commit | Change |
|---|---|---|---|
| 2.9.1 | 20/09/2026 18:15:33 | `8ca2380` | • Update the demo image |
| 2.9.0 | 20/09/2026 18:14:15 | `c36888d` | • The identity line now shows how you're paying as its own first item — `Sub | ◆ Opus 5` instead of `◆ Sub Opus 5` — so the billing mode reads as a badge of its own rather than part of the model name. It can be moved or dropped like any other item in a custom layout, under the name `mode`.<br>• Also lands the repository housekeeping that had been sitting uncommitted: contributor and maintainer docs, a code of conduct, support and release guides, CODEOWNERS, the release and Claude GitHub workflows, extra test files, and a note recording an idea that was considered and turned down. |
| 2.8.0 | 19/09/2026 13:01:33 | `f14e646` | • The identity line now says how you're paying before the model name: `API` when the account runs on prepaid credits, or the subscription tier (`Pro`, `Max 20x`, or plain `Sub`) when it doesn't. Detected automatically, hidden behind a proxy where it would be misleading, and overridable with a new `plan_label` setting.<br>• API-billing users get a credit bar next to the usage bars, the same shape subscribers get: how much of the balance is spent, how many dollars are left, and when it was last anchored to reality.<br>• New `/super-status:credits 102` command declares that balance in one line, so recording a top-up takes one command instead of a config edit. Accepts a backdate for a top-up recorded late.<br>• Spend comes from Anthropic's cost report when an admin key is available, and otherwise from your own Claude Code history priced at list rates — marked `est.` so an estimate never reads as an exact figure. That means the bar works on an individual account, which can't get an admin key at all.<br>• The balance snapshot records the time of day, not just the date. A balance read in the afternoon is true at that moment, so treating it as a midnight figure would have made the bar subtract the morning's spending twice.<br>• A message that appears in more than one transcript, which happens whenever a session is resumed or forked, is now counted once. Counting every copy had been inflating the estimate by almost half.<br>• The credit figure is fetched in the background, so the statusline never waits on the network or a history scan — it prints the last result it has and quietly refreshes. |
| 2.7.0 | 19/09/2026 12:20:16 | `3f1d66f` | • The statusline now also works inside Google's Antigravity CLI (agy), not just Claude Code — the README explains how to switch it on, and doctor.sh sets it up for you automatically when it sees Antigravity installed.<br>• The model name can now show the session's reasoning-effort level right after it, e.g. "Opus 5 [High]". Models that don't take an effort setting show nothing, and the new "effort" display switch turns the badge off.<br>• Fixed model names on Amazon Bedrock: names carrying a multi-part provider prefix (like "us.anthropic.") are now cleaned up fully instead of only partly.<br>• The GitHub Actions checkout step was updated from version 4 to version 7.<br>• These changes came from contributors Ira Abramov (pull requests #2 and #3) and Dependabot (pull request #1); this row records them in the ledger, which the merges on GitHub had skipped. |
| 2.6.1 | 11/09/2026 22:34:51 | `bd1d0b6` | • The auto-version hook now pushes normally instead of force-pushing, so it keeps working now that the main branch is protected against force-pushes and deletion on GitHub. |
| 2.6.0 | 11/09/2026 22:32:44 | `e06a92b` | • The model name on the first status line can now show the model's parameter count next to it, e.g. "Sonnet 5 (719B)".<br>• The numbers are yours to declare: a new "model_params" setting in config.json maps part of a model's name to the text shown in brackets, and the most specific match wins.<br>• Models you haven't listed are displayed exactly as before. |
| 2.5.1 | 29/08/2026 13:29:14 | `1ca216d` | • Add a version manager: every commit now records a plain-language version row in versions/2026.md, tags the release, and pushes with --force-with-lease. Added scripts/save.sh one-shot (stage + commit + version + push) and documented it in CLAUDE.md. |
